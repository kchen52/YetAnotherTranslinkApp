<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".MapsActivity" >
    <fragment xmlns:map="http://schemas.android.com/apk/res-auto"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.kchen52.yetanothertranslinkapp.MapsActivity"
        tools:layout="@layout/activity_maps" />

    <Button
        android:id="@+id/requestInfoBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:onClick="requestInformation"
        android:layout_marginEnd="10dip"
        android:layout_marginBottom="40dip"
        android:text="Request bus info"
        android:drawableStart="@mipmap/loop_black_24dp" />
    <TextView
        android:id="@+id/lastRequestDateTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="15dip"
        android:layout_marginBottom="25dip"
        android:text="Last updated: Never" />
</RelativeLayout>
